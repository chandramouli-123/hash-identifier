## Day 1
- Wrote v1 of hash-identifier in a single file.
- hashcandidate was written to produce output.

## Day 2

- Rule dataclass was written to enforce rules but HashCandidate was used to print output for simplicity
- refactoring the entire codebase into different modules from single file called hash-identifier
- Moved confidence into Rule so the detector doesn't hardcode it.


